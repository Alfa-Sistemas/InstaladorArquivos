ALTER TABLE C000048
ADD NUMERO_FISCAL VARCHAR(50);

UPDATE C000048 SET NUMERO_FISCAL = NUMERO_CUPOM_FISCAL;

update RDB$FIELDS set
RDB$FIELD_LENGTH = 500,
RDB$CHARACTER_LENGTH = 500
where RDB$FIELD_NAME = 'RDB$5662';

